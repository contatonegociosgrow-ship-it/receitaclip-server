[phases.setup]
nixPkgs = ["ffmpeg", "python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["which ffmpeg && ffmpeg -version"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
